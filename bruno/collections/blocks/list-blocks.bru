meta {
  name: List Blocks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/blocks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
  res.body.blocks: isArray
  res.body.total: isNumber
}
