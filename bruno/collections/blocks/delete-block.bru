meta {
  name: Delete Block
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/blocks/{{createdBlockId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 204
}
